[package]
name = "fortunate-primes"
version = "0.1.0"
edition = "2021"
description = "Calculate Fortunate numbers F(n) using PARI/GP"
license = "MIT"

[dependencies]
num_cpus = "1.16"
crossbeam-channel = "0.5"

[dev-dependencies]
